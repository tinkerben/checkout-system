"use strict";
exports.__esModule = true;
var models_1 = require("./models");
var typeGuards_1 = require("./typeGuards");
exports.runValidations = function (fns, input) {
    var validateResults = fns.map(function (fn) {
        var result;
        try {
            result = fn(input);
        }
        catch (error) {
            result = new models_1.Invalid(error.message);
        }
        return result;
    });
    var errors = [];
    validateResults.forEach(function (eos) {
        if (typeGuards_1.isStrictInvalid(eos))
            errors.push(eos);
    });
    return errors.length > 0 ? errors : input;
};
//# sourceMappingURL=syncRun.js.map